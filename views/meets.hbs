<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/mainStyle.css">
    <link rel="stylesheet" href="/css/meetsStyle.css">
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu+Mono:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <title></title>
</head>

<body>
    <header class="header">
        <div class="header__container">
            <div class="header__logo">CRM AI Mini App</div>

            <div class="header__nav nav">
                    <div class="nav__item"><a href="/{{userId}}" class="nav__text">Задачи</a></div>
                    <div class="nav__item"><a href="/contacts/{{userId}}" class="nav__text">Контакты</a></div>
                    <div class="nav__item nav__item_active"><a href="/meets/{{userId}}" class="nav__text">Встречи</a></div>
            </div>
        </div>
    </header>

    <section class="main-list">
        <div class="main-list__container">
            {{#each meet}}
                <a href="/meet/{{this.meeting_id}}/{{this.user_id}}" class="main-list__item item">
                    <div class="item__left-part">
                        <div class="item__title">{{this.title}}</div>
                        <div class="item__owner item__subtitle">{{this.username}}</div>
                    </div>
                    <div class="item__right-part">
                        <div class="item__place">{{this.location}}</div>
                        <div class="item__dates">{{dateFormat this.start_time "D.M.YYYY HH.MM"}} {{dateFormat this.end_time "D.M.YYYY HH.MM"}}</div>
                    </div>
                </a>
            {{/each}}

            <form action="/add-meet/{{userId}}/new" method="get" class="main-list__meet-adding">
                <button class="main-meet__adding-button">
                    Новая встреча
                </button>
            </form>
        </div>
    </section>
</body>
</html>